[build]
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma db push && npx prisma db seed && node dist/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
